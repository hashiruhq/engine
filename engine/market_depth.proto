syntax = "proto3";
package engine;

message MarketDepthPriceLevel {
  uint64 Price = 1;
  uint64 Volume = 2;
}

message MarketDepth {
  repeated MarketDepthPriceLevel Bid = 1;
  repeated MarketDepthPriceLevel Ask = 2;
  uint64 LastPrice = 3;
  uint64 LastVolume = 4;
  string Market = 5;
}